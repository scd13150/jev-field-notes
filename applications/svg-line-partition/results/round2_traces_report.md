# 第二轮：把绘画痕迹喂给 Jev，能换回多少分层与部件划分

模型 `jev-1.13.0`；15 个「画 × 痕迹条件」组合，判定 2,600 条，输入 964,599 tokens、输出 137,681 tokens（输出不计费），按 \$42/Btok（=\$0.042/Mtok）整个网格约 **\$0.041**，即一幅 22 笔的画做完一整套判定约 \$0.0027；服务端累计 41s。

问题问的是机器重绘 / live2d 真正要的四件事：**部件归属**（这条线属于哪个部件）、**层级角色**（它是主体、细节、装饰、背景还是辅助线）、**区域划分**（画布被切成几块，这条线主要落在哪块）、**运动归属**（动画时谁带着它走），外加一条 Noul：**这根开口的线是不是可以随时挪走装饰**。成对题问两条线是否同部件、是否同运动件、是否连笔、谁先谁后。

第一轮消融的是几何怎么写，那一轮的混淆项（`<g>` 分组树）这轮拿掉了：三幅新画全都只有一层 `<g>` 用来统一描边样式，文件里没有任何部件层级可抄。这一轮消融的是**这幅画是怎么被画出来的这件事怎么写**。

---

## 0. 五种痕迹条件与三幅画的挑战

| 条件 | state 里有什么 | 想分开的是什么 |
|---|---|---|
| `order_named` | 数组顺序 = 笔迹顺序 + `trace_ms` + 元素 id 就是笔画名 | 名字与痕迹同时给（上界） |
| `order_list` | 数组顺序 = 笔迹顺序 + `trace_ms`，id 换成 `s01…` | 只剩痕迹，名字拿掉 |
| `times_only` | 顺序是位置排序（与笔迹无关），`trace_ms` 是真时间；note 明说顺序要自己按数字比 | 顺序不写在数组位置上，要它自己算 |
| `order_none` | 没有 `trace_ms`，note 明说这里没有绘画顺序 | 什么痕迹都不给 |
| `order_scrambled` | 数组顺序与时间戳都是**假的**但自洽，note 一样叫它信 | 假证据（false-evidence 控制组） |

三幅画各带一个难点，正是要求的「非闭合装饰线」和打乱顺序：

| 画 | 笔画数 | 挑战 |
|---|---|---|
| `trace_stick` | 21 | 按部位顺序画：一个部位的几笔连着画完才换下一个，痕迹与部件基本重合 |
| `trace_house_interleaved` | 23 | 打乱着画：栅栏三根柱子被门、屋顶、窗的活儿隔开，瓦片夹在窗之间 |
| `trace_cat_decor` | 23 | 装饰陷阱：五根一模一样的开曲线，有的算毛有的算胡须，笔顺里还交替出现 |

区域真值不是标出来的，是代码按**每幅画自己的分块**算的：把每条线沿弧长每 1 点采一次样，看哪一块拿走了一半以上的长度，拿不走就是 `spans-several`。分块的矩形连同名字一起写进 state（`state.tiles`），所以它做的是一个有定义的空间划分，不是猜九宫格。

两个代码基线跟着每一格一起算，命中率要对着它们读：

- `geom`：闭合轮廓包含 + 6 点邻近兜底 + 一张手写的「轮廓→部件」表；
- `pen_run`：连贯性赌注——笔落点走超过 25 点就断成新一段，段内继承第一个能放下的部件名。

---

## 1. 一眼总览

**逐元素命中率**：三幅平均；单幅见 §2

| 维度 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `part` | 1.000 | 0.781 | 0.749 | 0.779 | 0.752 |
| `layer` | 0.701 | 0.554 | 0.614 | 0.628 | 0.556 |
| `region` | 0.908 | 0.923 | 0.909 | 0.894 | 0.940 |
| `motion` | 0.776 | 0.679 | 0.705 | 0.706 | 0.661 |

**多数类基线（恒定答最常见的那一类）**

| 维度 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `part` | 0.178 | 0.178 | 0.178 | 0.178 | 0.178 |
| `layer` | 0.402 | 0.402 | 0.402 | 0.402 | 0.402 |
| `region` | 0.343 | 0.343 | 0.343 | 0.343 | 0.343 |
| `motion` | 0.342 | 0.342 | 0.342 | 0.342 | 0.342 |

**宏 F1**

| 维度 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `part` | 1.000 | 0.739 | 0.645 | 0.663 | 0.699 |
| `layer` | 0.722 | 0.518 | 0.569 | 0.581 | 0.493 |
| `region` | 0.825 | 0.847 | 0.821 | 0.766 | 0.874 |
| `motion` | 0.687 | 0.605 | 0.736 | 0.655 | 0.655 |

**恒定回答的宏 F1 基线**

| 维度 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `part` | 0.030 | 0.030 | 0.030 | 0.030 | 0.030 |
| `layer` | 0.134 | 0.134 | 0.134 | 0.134 | 0.134 |
| `region` | 0.081 | 0.081 | 0.081 | 0.081 | 0.081 |
| `motion` | 0.077 | 0.077 | 0.077 | 0.077 | 0.077 |

**平均置信度**

| 维度 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `part` | 0.976 | 0.629 | 0.612 | 0.625 | 0.593 |
| `layer` | 0.767 | 0.628 | 0.623 | 0.624 | 0.616 |
| `region` | 0.879 | 0.881 | 0.863 | 0.848 | 0.889 |
| `motion` | 0.865 | 0.481 | 0.472 | 0.471 | 0.436 |

**高置信（≥0.85）覆盖率**

| 维度 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `part` | 0.971 | 0.320 | 0.210 | 0.273 | 0.178 |
| `layer` | 0.557 | 0.153 | 0.227 | 0.211 | 0.152 |
| `region` | 0.759 | 0.745 | 0.654 | 0.670 | 0.728 |
| `motion` | 0.686 | 0.032 | 0.032 | 0.048 | 0.000 |

**成对题命中率**

| 维度 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `p_same_part` | 0.819 | 0.724 | 0.768 | 0.768 | 0.761 |
| `p_same_motion` | 0.823 | 0.735 | 0.698 | 0.753 | 0.672 |
| `p_adjacent` | 1.000 | 1.000 | 0.493 | 0.474 | 0.475 |
| `p_after` | 1.000 | 1.000 | 0.872 | 0.525 | 0.570 |

读法：`region` 一行几乎不动，因为它只吃坐标，与痕迹无关——这一行是这次实验的内对照，说明五个条件真的只动了痕迹这一件事。`part` 一行在 `order_named` 上三幅全对，是这次最强的一格；但同一行在 `order_list` 上就掉，说明那一格是**名字与痕迹同时给**才拿到的。

---

## 2. 部件归属：名字才是主因，痕迹只补一小块

先把 1.000 那一格拆开。每幅画部件类数不同，所以逐幅看命中率：

**`part` 命中率**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 1.000 | 0.952 | 0.857 | 0.857 | 0.952 |
| `trace_house_interleaved` | 1.000 | 0.696 | 0.652 | 0.696 | 0.522 |
| `trace_cat_decor` | 1.000 | 0.696 | 0.739 | 0.783 | 0.783 |

**`part` 多数类基线**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.143 | 0.143 | 0.143 | 0.143 | 0.143 |
| `trace_house_interleaved` | 0.174 | 0.174 | 0.174 | 0.174 | 0.174 |
| `trace_cat_decor` | 0.217 | 0.217 | 0.217 | 0.217 | 0.217 |


- **名字值多少**（`order_named` 对 `order_list`，两者都有真笔迹，只差 id 是不是笔画名）：三幅各 +0.048、+0.304、+0.304。平均从 1.000 掉到 0.781。
- 名字+顺序同时给时三幅全对（宏 F1 1.000）。部件是 10–11 类的问题，多数类基线只有 0.143/0.174/0.217，不是撞对的。
- **痕迹值多少**（`order_list` 对 `order_none`，两者都没名字，只差有没有笔迹）：火柴人 +0.095、房子 +0.000、猫 -0.087，三幅平均 +0.003（0.781 对 0.779）。**顺序写在数组位置上、时间戳是真的，这些加起来值不到一笔。**
- 顺序不写在数组位置上、只留一串真时间戳（`times_only`）平均 0.749，与 `order_none` 的 0.779 同样几乎无差：逐幅 +0.000、-0.044、-0.044。要它自己从数字里把顺序算出来，算得出来（§7 的 p_after），但算出来的顺序并没有换成更好的部件划分。
- **假痕迹是负资产**（`order_scrambled` 对 `order_none`）：火柴人 +0.095，是正的——那幅画本来就是一个部位连着画完，任意一套自洽的假顺序里同一部位的笔画多半仍然挨在一起；猫 +0.000（打平）；而打乱那幅房子 -0.174，从 0.696 掉到 0.522。而假痕迹那格的置信度并没有一起掉：整体平均 0.593，**答错时平均仍有 0.497**，比什么都没给时答错的 0.417 还高一点。

### 2.1 划分质量：ARI 会骗人，NMI 不会

把每个部件的软分布折成 P(同部件)=Σ p_a·p_b，再单链接聚到 0.5，与真值划分比：

**`part` 软划分的 ARI**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 1.000 | 0.889 | 0.754 | 0.678 | 0.617 |
| `trace_house_interleaved` | 1.000 | 0.084 | 0.560 | 0.523 | 0.074 |
| `trace_cat_decor` | 1.000 | 0.350 | 0.350 | 0.350 | 0.350 |

**`part` 软划分的 NMI**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 1.000 | 0.977 | 0.943 | 0.930 | 0.917 |
| `trace_house_interleaved` | 1.000 | 0.843 | 0.877 | 0.856 | 0.828 |
| `trace_cat_decor` | 1.000 | 0.852 | 0.852 | 0.852 | 0.852 |

**聚出的簇数**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 11 | 10 | 12 | 13 | 14 |
| `trace_house_interleaved` | 10 | 22 | 16 | 15 | 21 |
| `trace_cat_decor` | 10 | 20 | 20 | 20 | 20 |

**真值部件数**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 11 | 11 | 11 | 11 | 11 |
| `trace_house_interleaved` | 10 | 10 | 10 | 10 | 10 |
| `trace_cat_decor` | 10 | 10 | 10 | 10 | 10 |


`order_named` 三幅 ARI 全 1.000/1.000/1.000，簇数 11/10/10 与真值一致，混簇数 0/0/0 —— 这就是「把名字和笔迹一起给它，划分可以直接落库」的意思。

但另几列要小读：`order_list` 在房子那幅 ARI 只有 0.084，NMI 还有 0.843，簇数却是 22（23 笔里几乎一笔一簇）。**这不是聚错了，是不敢聚**：top-1 已经有 0.696 对，但质量摊得开，Σ p_a·p_b 过不了 0.5 的合并门槛。所以 ARI 在这里同时度量了「对不对」和「分布尖不尖」，读它必须连着簇数一起读。

### 2.2 对着代码基线读

两条规则只吃几何和自己的分段逻辑，与痕迹条件无关，所以按画列一次就够：

| 画 | Jev `part`（名字+顺序） | Jev `part`（只有顺序） | 包含规则 `geom` | 连贯规则 `pen_run` | 多数类 |
|---|---|---|---|---|---|
| `trace_stick` | 1.000 | 0.952 | 0.095 | 0.143 | 0.143 |
| `trace_house_interleaved` | 1.000 | 0.696 | 0.478 | 0.478 | 0.174 |
| `trace_cat_decor` | 1.000 | 0.696 | 0.087 | 0.087 | 0.217 |

三幅平均：包含规则 0.220，连贯规则 0.226，比多数类基线 0.178 只高出 0.042 和 0.048。也就是说这次不是「模型比规则强一点点」，而是规则根本没有把开口装饰线放对的能力：火柴人那幅包含规则 0.095，因为 14/21 笔它答不出来（没有轮廓可依托）；打乱那幅房子反而有 0.478，因为它的部件本来就靠「在不在屋顶里、在不在门里」区分。

顺带一个反直觉的数字：笔迹走位距离当「同部件」判据其实相当好——三幅的 AUC 0.929–0.967（同段中位 32.4 点，跨段中位 100.2 点）。可 `pen_run` 就是拿不准：一个部件只有一笔时**也需要名字**，而连贯规则只会把没有轮廓可依托的线并进上一笔的名字里；同时没有任何单一阈值能把同段/跨段分开（中位 32 对 100，但分布重叠，25 这个数是这组数据上的折中）。

---

## 3. 区域划分：与痕迹无关，而且给矩形就能算

**`region` 命中率**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.810 | 0.857 | 0.857 | 0.857 | 0.905 |
| `trace_house_interleaved` | 0.957 | 0.913 | 0.913 | 0.913 | 0.957 |
| `trace_cat_decor` | 0.957 | 1.000 | 0.957 | 0.913 | 0.957 |

**`region` 宏 F1**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.706 | 0.750 | 0.750 | 0.750 | 0.854 |
| `trace_house_interleaved` | 0.848 | 0.792 | 0.792 | 0.792 | 0.848 |
| `trace_cat_decor` | 0.920 | 1.000 | 0.920 | 0.757 | 0.920 |


区域块是每幅画自己切的（10 块 / 7 块 / 6 块，都是长条+主体密区），真值由代码按弧长采样算。五个条件的差别在噪声内：`order_named` 0.908 对 `order_none` 0.894 对 `order_scrambled` 0.940，多数类基线 0.343。这说明它做的是**「沿弧长比多少」这类空间判断**，不需要语义也能做，顺序被造假也不影响它。反过来，第一轮里 `scrambled` 把区域打到 0.00–0.22，这一轮假的是**顺序**不是坐标，区域一行自然不动——这正好是这次消融设计的自检。

---

## 4. 层级角色：最弱的一维，而且最吃名字

**`layer` 命中率**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.667 | 0.619 | 0.667 | 0.667 | 0.667 |
| `trace_house_interleaved` | 0.652 | 0.565 | 0.652 | 0.696 | 0.565 |
| `trace_cat_decor` | 0.783 | 0.478 | 0.522 | 0.522 | 0.435 |

**`layer` 多数类基线**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.381 | 0.381 | 0.381 | 0.381 | 0.381 |
| `trace_house_interleaved` | 0.391 | 0.391 | 0.391 | 0.391 | 0.391 |
| `trace_cat_decor` | 0.435 | 0.435 | 0.435 | 0.435 | 0.435 |

**`layer` 高置信覆盖率**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.714 | 0.286 | 0.333 | 0.286 | 0.238 |
| `trace_house_interleaved` | 0.435 | 0.087 | 0.087 | 0.087 | 0.130 |
| `trace_cat_decor` | 0.522 | 0.087 | 0.261 | 0.261 | 0.087 |


五档角色（backdrop/body/detail/decor/guide）平均命中从 0.701（`order_named`）到 0.554（`order_list`），是四个 Choice 维度里最低的一族，基线 0.402——最好那格也只比基线高 0.299。它也是最吃名字的一维：猫那幅从 0.783 掉到 0.478，高置信覆盖率同时从 0.522 塌到 0.087；全三幅的覆盖率则从 0.557 到 0.153。**覆盖率跟着掉是好消息**：这一维它知道自己没依据。

失误集中在 `decor` 与 `detail` 之间：瓦片、裂纹、衣褶、胡须都算「开口的小线」，作者一边归为装饰、一边归为细节。这两档在真实管线里本来就是同一个动作（要不要单独一层），所以工程上更稳的做法是**只问「这条线能不能单独一层」，别问它属于五档里的哪一档**——也就是下面第 5 节那条 Noul。

---

## 5. 装饰性非闭合线：一条能用的分流

用户点名的挑战就是这种线：五根一样的毛、瓦片、烟、草。问法是给出一句断言让它估概率（Noul）：「这是一根为了好看而画的开口线，单独重画成一个小浮动层不会伤到主体结构」。

| 条件 | 命中率 | 常数答基线 | recall | precision | AUC | 真为正的均值 | 真为负的均值 |
|---|---|---|---|---|---|---|---|
| 名字+顺序 | 0.835 | 0.674 | 1.000 | 0.657 | 0.972 | 0.852 | 0.214 |
| 只有顺序 | 0.731 | 0.674 | 0.933 | 0.538 | 0.925 | 0.674 | 0.309 |
| 只有时间戳 | 0.877 | 0.674 | 0.886 | 0.755 | 0.921 | 0.586 | 0.238 |
| 什么都没给 | 0.877 | 0.674 | 0.886 | 0.755 | 0.935 | 0.620 | 0.251 |
| 假痕迹 | 0.831 | 0.674 | 0.933 | 0.670 | 0.920 | 0.615 | 0.291 |

三幅一共 22 根开口装饰线（真值），共 67 笔，`order_named` 条件下recall 1.000、AUC 0.972，真为正的均值 0.852 对真为负的均值 0.214 —— 分隔很开。逐幅 recall 1.000/1.000/1.000，precision 0.556/0.583/0.833。

命中率之所以看着只比常数答（0.674）高 0.161，是因为真值本身就偏（多数线确实不是装饰），**这条题该看 recall 和 AUC，不该看命中率**。

把 `order_named` 下 noul≥0.5 而真值为负的笔画列出来，误报长这样：

| 画 | 误报（noul） | 真漏报 |
|---|---|---|
| `trace_stick` | `guide-axis` 0.65、`ear-l` 0.54、`shirt-fold-1` 0.77、`shirt-fold-2` 0.77 | 无 |
| `trace_house_interleaved` | `fence-post-1` 0.64、`fence-post-2` 0.58、`fence-rail` 0.52、`fence-post-3` 0.59、`wall-crack` 0.90 | 无 |
| `trace_cat_decor` | `fly-body` 0.80、`ground-line` 0.69 | 无 |

误报分两种，工程后果不一样。`shirt-fold`、`wall-crack`、`fly-body` 这种「本来就是一根开口的小线」被说成可单独一层，按 live2d 的做法反而对——衣褶和墙裂本来就该跟自己的部件走一个浮动层。但 `fence-post-1..3` 加 `fence-rail` 四笔全被点名，就是真错了：栅栏是承重的结构件，拔掉它画面就少一个东西。`ear-l` 0.54 这种更值得警惕：分数刚过线、东西却是主体的耳朵。`guide-axis`（辅助线）和 `ground-line`（地平线）被判成正例，是它比作者更对——辅助线本来就不该进重绘结果，地面线本来就该单独一层。所以结论是：**这条 Noul 能当筛选器（分数排序 + 卡阈值）用，阈值以下一定能安全删，阈值附近必须再过一遍部件归属——一根线「开口」不等于它「不重要」。**

---

## 6. 运动归属（rigging）

**`motion` 命中率**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.762 | 0.905 | 0.810 | 0.857 | 0.810 |
| `trace_house_interleaved` | 0.783 | 0.261 | 0.565 | 0.435 | 0.348 |
| `trace_cat_decor` | 0.783 | 0.870 | 0.739 | 0.826 | 0.826 |

**`motion` 多数类基线**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.286 | 0.286 | 0.286 | 0.286 | 0.286 |
| `trace_house_interleaved` | 0.261 | 0.261 | 0.261 | 0.261 | 0.261 |
| `trace_cat_decor` | 0.478 | 0.478 | 0.478 | 0.478 | 0.478 |

**`motion` 软划分 ARI**

| 画 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 |
|---|---|---|---|---|---|
| `trace_stick` | 0.390 | 0.858 | 0.547 | 0.547 | 0.402 |
| `trace_house_interleaved` | 0.620 | 0.000 | 0.000 | 0.000 | 0.000 |
| `trace_cat_decor` | 0.783 | 0.182 | 0.182 | 0.239 | 0.067 |


平均 0.776（`order_named`）对 0.706（`order_none`），基线 0.342。运动件比部件少（部件 10–11 类，运动 6–7 件），所以这一维更像一个粗一格的划分，高置信覆盖率在 `order_named` 下有 0.686，`order_list` 下塌到 0.032。

这一维是三幅里最不稳的，两个方向都要报：房子那幅没有名字时（`order_list` 0.261）**正好落在自己的多数类基线上（0.261）**——不比「恒定答最常见的那个运动件」多知道任何事；同一幅给名字有 0.783，给假顺序只有 0.348，连 0.435（什么都不给）都不如。但反过来，火柴人那幅 `order_named` 只有 0.762，低于它自己 `order_none` 的 0.857——21 笔里两笔的差别。**所以运动归属只读方向（有名字 > 没名字，假顺序 < 没顺序），不读幅度。**

---

## 7. 成对题：读得出顺序，和会用顺序，还是两件事

每一对都问四个 Noul，方向题按镜像配平（同一对正反各问一次），所以命中率高于 0.5 才不是猜方向：

| 题 | 名字+顺序 | 只有顺序 | 只有时间戳 | 什么都没给 | 假痕迹 | 基线 | 正例数（三幅平均） |
|---|---|---|---|---|---|---|---|
| `p_same_part` | 0.819 | 0.724 | 0.768 | 0.768 | 0.761 | 0.614 | 6 |
| `p_same_motion` | 0.823 | 0.735 | 0.698 | 0.753 | 0.672 | 0.644 | 8 |
| `p_adjacent` | 1.000 | 1.000 | 0.493 | 0.474 | 0.475 | 0.551 | 8 |
| `p_after` | 1.000 | 1.000 | 0.872 | 0.525 | 0.570 | 0.525 | 7 |

- **谁先谁后**：数组顺序真是笔迹时 1.000；换成只有时间戳（note 已明说顺序要自己比数字）仍有 0.872，三幅分别 0.846/0.769/1.000 —— 会读时间。但平均绝对信心一起掉：偏离 0.5 的程度从 0.469 降到 0.306。
- **是不是连笔**：同一个时间戳条件下只有 0.493，三幅 0.385/0.538/0.556，**低于自己的常数答基线 0.551**。也就是说它能把两笔排前后，却不能从一串时间里认出「这一笔之后紧接着那一笔、中间没有别的」——那需要对所有其它数字做一次最近邻比较。
- **什么都没给**时两条方向题的均值都掉到 0.115 / 0.069，即它按 criteria 里写的「state 没说就答 false」干脆否认。这是设计里的正确行为，不是失败：所以那两格的命中率（0.525、0.474）度量的是**问题本身能不能答**，不是它推理得好不好。
- **假痕迹**：`order_scrambled` 的 p_after 平均 noul 抬到 0.557（真痕迹下 0.484），方向真值也跟着翻，而命中率只有 0.570 —— 它照读了那条假时间线，读得很有信心，读到的是错的。
- **同部件的成对题比逐元素题差**：`p_same_part` 在 `order_named` 下平均 0.819，而同一批笔画的 `part` 是 1.000。逐元素答案与成对答案一致率：三幅 1.000/0.846/0.611，整套条件平均最低到 0.538：

两条路问的是同一件事，但只有一条带着词表。猫的 `fur` vs `whiskers`、`ear-left` vs `head` 这类区分在没有词表时就退化成「两根线挨在同一团东西上，算不算一个部件」。工程含义：**要做部件划分就问带名字的 Choice；成对 Noul 适合当复核，不适合当主判据。**

---

## 8. 整图判断

三条整图题各问一次（每幅画一行）。档位题的「预测」按 火柴人/房子/猫 顺序列三个档号，「真值」同理：

| 条件 | 顺序可读（noul） | 运动件数 预测 | 真值 | 对 | 装饰占比 预测 | 真值 | 对 |
|---|---|---|---|---|---|---|---|
| 名字+顺序 | 0.97 | 2/0/2 | 2/2/2 | 2/3 | 1/1/1 | 1/1/1 | 3/3 |
| 只有顺序 | 0.97 | 2/3/2 | 2/2/2 | 2/3 | 1/1/3 | 1/1/1 | 2/3 |
| 只有时间戳 | 0.89 | 2/3/2 | 2/2/2 | 2/3 | 1/1/1 | 1/1/1 | 3/3 |
| 什么都没给 | 0.03 | 2/3/2 | 2/2/2 | 2/3 | 1/1/1 | 1/1/1 | 3/3 |
| 假痕迹 | 0.97 | 2/3/3 | 2/2/2 | 1/3 | 3/1/1 | 1/1/1 | 2/3 |

`g_order_readable` 是唯一完全对的一列（15/15）。给顺序时 noul 0.97，假顺序时 0.97（它分不清真假，但它确实读到了「顺序是写得明的」，这句不算错），什么都不给时 0.03，只有时间戳时 0.89 —— 它不认为一串数字等于「读得出顺序」，这与 §7 里连笔那一格互证。

两条计数题继续犯第一轮的老毛病：**分档边界**。运动件真值三幅都是 2 档（4–6 件），15 格里答对 9；装饰占比真值也都是 1 档（「几笔装饰压在结构上」），答对 13。错的 8 格里，7 格往**高一档**偏，1 格往低档偏——它系统性把「有若干装饰线」读成「大半张画是装饰」。要能用的还是**连续分排序**，不是落库的档位。

---

## 9. 这份上限对机器重绘 / live2d 意味着什么

1. **给名字，给笔迹顺序，部件划分就能直接落库**：三幅 1.000 命中、宏 F1 1.000、ARI 1.000、混簇 0 个，对着的是 0.178 的多数类基线。这是这次实验里唯一一条「可以照抄进管线」的结论。
2. **笔迹顺序单独值不了多少**：把名字拿掉（`order_list`），平均从 1.000 掉到 0.781；而 `order_list` 对 `order_none` 只差 +0.003。**部件的身份主要来自这条线自己画的是什么、在哪里，不来自它是第几笔。**
3. **假痕迹是负资产**：房子那幅 0.522 对无痕迹的 0.696，置信度还不掉。**上游录不到真实笔迹时，宁可不给，也别给一个看起来自洽的顺序。**
4. **区域划分是免费的**：0.915 上下，五个条件都不动（基线 0.343），把矩形定义写进 state 就够了，不需要名字也不需要顺序。
5. **层级角色（五档）是这次最弱的一维**：0.610 的平均命中，且 decor/detail 本来就互相混淆。工程上用那条 Noul（能不能单独一层）替代，它的 AUC 有 0.972。
6. **顺序能排前后，不能认连笔**，所以「按笔画段自动分组」这种省事做法不要指望它（§7：p_after 0.872 对 p_adjacent 0.493）。要分段就用代码算走位距离——顺带说，走位距离本身在这三幅上是不错的判据（AUC 0.929–0.967），但那是可精确计算的事实，留在代码里。
7. **成对题与逐元素题会互相打脸**（`order_named` 下一致率 0.819，最差一格 0.538）：词表在手上时它分得开毛和胡须，只问「这两根算不算一个部件」时就算不开了。

落到 live2d 式的最小管线，这次支持的分工是：

```text
SVG/笔迹解析（代码）：点列、长度、闭合、包含、走位距离、时间戳排序、区域矩形
        ↓ 一个扁平 state，元素带 id（id 用有意义的部件名，别用 s01）
Jev（一次请求，全部并行）：part / region / motion 带词表的 Choice
                            + 非闭合装饰线 Noul（当筛选器，卡阈值）
        ↓
代码：Choice 分布 → 软划分（ARI/NMI 连着簇数一起看）→ 浮动层清单；p_* 只当复核信号
```

## 10. 这一轮的失效模式与设计缺陷

- **它不为痕迹的真伪负责**（§2、§7）：假时间线被照读，置信度不掉。
- **数组位置是它读顺序的首选通道**：顺序只在数组位置上时读得最好；只给时间戳时，前后关系还能推（p_after 0.872），连笔关系就废了（0.493，基线 0.551）。
- **ARI 混着度量了「分布尖不尖」**（§2.1），必须连簇数与 NMI 一起读。
- **命中率对偏斜真值没意义**：`loose_decor` 的常数答基线 0.674，`p_adjacent` 的基线 0.551，所以这两族要看 recall/precision/AUC。
- **`order_none` 的两条方向题是「不可答题」**：criteria 明确写了「state 没说就答 false」，那两格的命中率是问题的性质，不是它的能力。
- **样本还很小**：三幅共 67 笔、每幅 13 对成对题，单幅的 0.05 级差别就是一两笔的差别；跨条件的对比只读同幅内部差。
- **区域真值依赖我给的切法**：切法本身是代理定义，换一种切法要重测（第一轮已见过同一条规则在九宫格上成立、在自定切法上重排）。
- **词表是我提供的**：这既是结论也是局限——这次测的是「给定词表后能否归类」，不是「能否发明部件名」。

---

复现：`python -m src.selftest` → `python -m src.run_traces --check` → `python -m src.run_traces` → `python -m src.traces_report`。
第一轮的报告在 `results/report.md`，序列化条件对照见其 §0。
